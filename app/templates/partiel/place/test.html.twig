<div class="card">
    <div class="card-body">
        <h5 class="card-title">{{ story.place.name }}</h5>

        {{ include('shared/_alert.html.twig') }}

        <p>Test de {{ story.skill }} à {{ story.difficulty }}</p>

        <p><button
            hx-get="{{ path('app_partiel_place_test_result', {place_id: place.id, story_id: story.id}) }}"
            hx-trigger="click"
            hx-target="#story"
            class="btn btn-success"
        >
            Lancer le D20
        </button></p>

    </div>
</div>